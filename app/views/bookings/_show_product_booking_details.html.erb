<div class="container rounded">
  <div class="card rounded col-5 p-2 w-100 shadow-sm">
    <div class="d-flex justify-content-between align-items-baseline">
      <h3 class="p-2">Your Booking</h3>
      <div class="me-3">
        <%= render 'shared/status', booking_status: @booking.booking_status %>
      </div>
    </div>

    <div class="p-2">
      <p>Dates: <%= @booking.start_date.strftime('%d/%m/%Y') %> - <%= @booking.start_date.strftime('%d/%m/%Y') %></p>
      <p>Total Days: <%= ((@booking.end_date - @booking.start_date + 1)/ 86400).round(0) %> days</p>
    </div>
    <div class="border-bottom mx-3"></div>
      <div class="p-2">
      <p>Your Items:</p>
        <ul class="list-group px-2">
          <% @booking.product_assignments.each do |product_assignment| %>
                <li class="list-inline-item p-x-2">
                  <div class="d-flex">
                    <div class="flex-grow-1">
                      <p class="mb-0"><%= product_assignment.product.name %></p>
                      <p class="mb-1" style="font-size:12px;"><%= product_assignment.product.category.capitalize %></p>
                    </div>

                    <% unless product_assignment.product.product_type == "bike" %>
                      <p class="mb-0">$<%= product_assignment.product.price %></p>
                      <%= link_to product_assignment_path(product_assignment), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
                        <i class="fa-solid fa-trash ms-3"></i>
                      <% end %>
                    <% else %>
                      <p class="mb-0" style="margin-right:30px;">$<%= product_assignment.product.price %></p>
                    <% end %>
                  </div>
                </li>
          <% end %>
        </ul>
      </div>
    <div class="border-bottom mx-3"></div>
    <div class="p-2 pb-0">
      <p>Per day</p>
      <p>Total Price</p>
    </div>
    <% if @booking.booking_status == "draft" %>
      <form method="PATCH" action="<%= checkout_path(@booking) %>", class="">
        <input type="hidden" name="product_id" value="<%= params[:product_id] %>">
        <input type="submit", class="btn btn-primary w-100", value="Checkout & pay with cash">
        <a href="" class="btn btn-primary w-100 mt-3" data-bs-toggle="modal" data-bs-target="#exampleModal">Checkout & Pay with paypall</a>
          <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog">
              <div class="modal-content">
                <div class="modal-header">
                  <h5 class="modal-title" id="exampleModalLabel">Oooooooops...Paypall is down!</h5>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                  Just kidding...pay cash!
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
              </div>
            </div>
          </div>
      </form>
    <% end %>
  </div>
</div>
